import { ResponceFromYoutube } from './responce-from-youtube';

describe('ResponceFromYoutube', () => {
  it('should create an instance', () => {
    expect(new ResponceFromYoutube()).toBeTruthy();
  });
});
